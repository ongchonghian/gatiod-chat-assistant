> *This was generated by AI during triage.*

Status: needs-triage
Category: enhancement
Sprint tickets: V2-905, V2-906

# LLM extractor rollout to remaining systems + regex deletion

## V2-905 — Roll out to remaining 8 systems

After `upper_limb` calibration passes (V2-904), extend LLM extractor shadow + promotion to: `respiratory`, `renal` (simplest condition sets) → `gastro`, `hearing` → `lower_limb`, `spine` → `cns`, `visual` (most complex). Each system follows the same shadow → calibration → promote → remove-regex sequence.

**Blocker:** V2-904 (upper_limb calibration) — which is itself blocked on V2-701 (calibration runner).

## V2-906 — Delete regex extractors

Once the LLM extractor is primary for all 9 systems and one release cycle has passed without rollback, delete `src/v2/extractors/[system].ts` files and remove `SlotSignals` / regex imports. `slotSchemas/[system].ts` and `slotSchemas/[system]Extractor.ts` become the canonical path.

**Blocker:** V2-905 (full rollout complete).

## When to revisit

Break V2-905 into per-system subtasks once V2-904 closes. V2-906 gets its own issue once V2-905 is fully landed.

## Comments
