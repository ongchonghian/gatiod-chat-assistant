> *This was generated by AI during triage.*

Status: needs-info
Category: enhancement
Sprint ticket: V2-904

# Calibration run: LLM extractor vs regex baseline for upper_limb

## Summary

Run the Excel calibration workbook against `upper_limb` with the LLM shadow extractor active. Measure safe-outcome and exact-calc rates against the ADR-0001 thresholds. If thresholds are met, promote the LLM extractor to primary and retire the regex extractor for `upper_limb`.

## Blocker

**Blocked on V2-701 (Sprint 7, NOT STARTED)** — the calibration runner (`scripts/calibration/run.ts`) does not exist yet. The runner reads the `.xlsx` workbook, replays extraction → readiness → argBuilder → mocked tool chain per row, and emits a `SystemCalibrationReport`. Without it, there is no automated way to measure LLM extractor accuracy at scale.

Also requires a live Gemini API key in the calibration environment — the LLM extractor is a no-op when `getGeminiClient()` returns undefined.

## What needs to happen before this ticket can start

1. V2-701 (calibration runner) merged and runnable locally
2. Confirm the Excel workbook for `upper_limb` is up to date (same dataset used for the ADR-0001 promotion evidence)

## When to revisit

After V2-701 ships. At that point, promote to `ready-for-agent` with the workbook path and a brief covering the calibration runner invocation flags.

## Comments
