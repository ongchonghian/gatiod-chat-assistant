> *This was generated by AI during triage.*

Status: needs-triage
Category: enhancement
Sprint ticket: V2-809

# Excel semantic shadow reporting

## Summary

Add semantic outcome classes (`semantic_proposal_correct | partial | unsafe | failed`) to the shadow runner, track recall / fallback / semantic correction rate against a curated golden set and Excel-derived scenarios, and expose results via `GATIOD_RUN_SEMANTIC_SHADOW=true`.

## Blocker

Explicitly deferred until semantic consensus is in production (`SEMANTIC_CONSENSUS_ENABLED` default on). Before this ticket can start:

1. V2-805 (shadow grader wiring) must be merged — that delivers the runtime `gradeSemanticCase` call and audit events that this ticket's batch runner will aggregate.
2. A real production traffic set (or a representative Excel workbook) must exist to run against. This depends on semantic consensus handling live doctor inputs for at least one sprint cycle.

## When to revisit

After Sprint 8 rollout completes and the first round of `semantic_shadow_grade` audit events are collected. At that point, promote to `ready-for-agent` with a workbook path and outcome-class target thresholds as part of the brief.

## Comments
